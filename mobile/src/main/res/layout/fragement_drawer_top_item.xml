<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="160dp"
    android:orientation="vertical"
    android:background="@color/accent_"
    android:id="@+id/drawer_top">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/top_main_background_image"
        android:scaleType="centerCrop" />

    <View
        android:layout_width="match_parent"
        android:background="@drawable/darken_gradient_from_bottom"
        android:layout_height="56dp"
        android:layout_gravity="bottom" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_gravity="bottom"
        android:gravity="center_vertical|start"
        android:textSize="18sp"
        android:paddingLeft="16dp"
        android:paddingRight="48dp"
        android:id="@+id/top_text_view"
        android:textColor="@android:color/white" />

    <eu.laprell.timetable.widgets.RippleTouchImageView
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:scaleType="centerInside"
        android:visibility="invisible"
        android:src="@drawable/ic_expand_more_white_24dp"
        android:id="@+id/top_image_expand"
        android:layout_gravity="end|bottom" />

</FrameLayout>